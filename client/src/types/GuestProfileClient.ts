export interface FormFields {
    first_name: string;
    last_name: string;
    email: string;
}

export interface PasswordFields {
    oldPassword: string;
    newPassword: string;
    confirmPassword: string;
}